ALGORITHM InsertionSort(arr) 
VAR
    a:arr = [5, 2, 4, 6, 1, 3];
BEGIN
    (Insertion:=arr[1];i < arr.length; i++);\
    j = i - 1;
    WHILE (j >= 0 && arr[j] > key) DO
    IF (arr[j + 1] = arr[j];) THEN
    RETURN j--;
    END_IF
    j:=arr[j + 1] 
    END_WHILE
    
END